div.row-fluid
  div#info.span12.well 
    p Alert the room to your Github commits.

- each repo in usersRepos
  div.row-fluid  
    div.span12
      input(type="checkbox", class="subscribe", name='repo['+repo['full_name']+']')
      |  
      strong= repo.name
      |  
      - if (repo.private)
        i.icon-lock
      = " " + repo.description
      // = JSON.stringify(repo)

